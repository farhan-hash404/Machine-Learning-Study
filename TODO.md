# TODO: Create FastAPI app in create.py on dummy data (✅ COMPLETED)

## Steps:
1. [✅] Create TODO.md with plan steps
2. [✅] Create create.py with FastAPI app and dummy data endpoints
3. [✅] Update TODO.md to mark progress
4. [ ] Followup: Install deps (`pip install fastapi uvicorn[standard] pydantic`) and run `uvicorn create:app --reload` (opens on port 8001; visit http://127.0.0.1:8001/docs)

**Files created:**
- `create.py`: Full FastAPI app with in-memory dummy items data (10 samples), endpoints: GET /, GET/POST /items/, GET /items/{id}.

**Next:** Run the command below to start the server and test in browser.

